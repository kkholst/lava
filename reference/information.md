# Extract information matrix

Extract the (observed or expected) information matrix from a model
object.

## Usage

``` r
information(x, ...)
```

## Arguments

- x:

  Model object

- ...:

  Additional arguments to lower level functions

## Value

Information matrix or its inverse (depending on arguments).
